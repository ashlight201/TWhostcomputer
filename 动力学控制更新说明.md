# 阻抗控制算法更新说明

## 📝 更新内容

已将原有的简化阻抗控制算法改为**基于动力学模型的物理准确控制算法**。

---

## 🔧 核心改进

### 原来的算法（简化版）：
```cpp
// 计算控制指令：θ_command = θ_current + F / K
double commandAngle1 = m_currentAngle1 + impedanceForce1 / m_K;
double commandAngle2 = m_currentAngle2 + impedanceForce2 / m_K;
```

### 新算法（基于动力学）：
```cpp
// 1. 计算阻抗力矩
double impedanceTorque = K × Δθ + B × Δω + G

// 2. 根据动力学方程：τ = J × α，计算角加速度
double angularAcceleration = impedanceTorque / J

// 3. 对角加速度积分得到角速度：ω = ∫α dt
m_calculatedVelocity += angularAcceleration × dt

// 4. 对角速度积分得到角位置：θ = ∫ω dt
m_calculatedPosition += m_calculatedVelocity × dt

// 5. 计算控制指令
double commandAngle = m_currentAngle + m_calculatedPosition
```

---

## 📐 物理模型说明

### 动力学方程
```
τ = J × α
```
其中：
- **τ (tau)**: 输出力矩（阻抗力）
- **J**: 关节及负载的转动惯量（已知量）
- **α (alpha)**: 角加速度

### 运动学积分
```
α = τ / J                    (角加速度)
ω = ∫α dt                    (角速度)
θ = ∫ω dt                    (角位置)
```

### 控制输出
```
θ_command = θ_current + θ_calculated
```

---

## 🆕 新增参数

### 转动惯量参数
- **J1**: 关节1的转动惯量（kg·m²）
- **J2**: 关节2的转动惯量（kg·m²）

这些参数需要根据实际机械结构和负载计算得出。

### UI界面新增
在"阻抗参数设置"区域新增了两个输入框：
- **转动惯量1 (J1)**: 默认值 0.1
- **转动惯量2 (J2)**: 默认值 0.1

---

## 📊 数值积分说明

### 积分方法
采用**欧拉法（Euler Method）**进行数值积分：
```
v(t+dt) = v(t) + a(t) × dt
θ(t+dt) = θ(t) + v(t) × dt
```

### 时间步长
- 控制频率：50Hz
- 时间步长：dt = 0.02秒（20ms）
- 具有异常检测，防止时间步长过大

### 积分初始化
在捕获初始位置时，重置所有积分状态：
```cpp
m_calculatedVelocity1 = 0.0;
m_calculatedVelocity2 = 0.0;
m_calculatedPosition1 = 0.0;
m_calculatedPosition2 = 0.0;
```

---

## 🎯 物理意义

### 为什么这样更好？

1. **物理准确性**
   - 符合牛顿第二定律（旋转形式）
   - 考虑了实际的转动惯量
   - 力矩与加速度的关系更真实

2. **控制平滑性**
   - 通过积分产生平滑的运动轨迹
   - 避免突变和抖动
   - 更符合实际机械系统的动态响应

3. **可调性强**
   - 通过调整转动惯量参数适应不同负载
   - 独立控制每个关节的动力学特性
   - 更容易调试和优化

---

## 📁 修改的文件

### 1. motorcontrol.h
- 新增成员变量：`m_J1`, `m_J2`（转动惯量）
- 新增成员变量：`m_calculatedVelocity1/2`（计算的角速度）
- 新增成员变量：`m_calculatedPosition1/2`（计算的角位置）
- 修改函数签名：`setImpedanceParameters(K, B, G, J1, J2)`

### 2. motorcontrol.cpp
- 修改构造函数：初始化新变量
- 修改 `setImpedanceParameters()`：接受并保存J1, J2
- 修改 `captureInitialPosition()`：重置积分状态
- **核心修改** `controlLoop()`：实现基于动力学的控制算法

### 3. mainwindow.h
- 新增UI组件：`Inertia1_lineEdit`, `Inertia2_lineEdit`

### 4. mainwindow.cpp
- 修改 `createParameterGroup()`：添加转动惯量输入框
- 修改 `onConfirmClicked()`：读取并验证转动惯量参数

---

## 🔍 关键代码片段

### 控制循环核心（motorcontrol.cpp 第210-259行）

```cpp
void MotorControl::controlLoop()
{
    // 获取时间步长
    double dt = (currentTime - m_lastTime) / 1000.0;
    
    // 计算位置和速度误差
    double positionError1 = m_currentAngle1 - m_initialAngle1;
    double velocityError1 = m_currentVelocity1;
    
    // 计算阻抗力矩
    double impedanceTorque1 = K × positionError1 + B × velocityError1 + G;
    
    // === 基于动力学模型的控制算法 ===
    // 1. 计算角加速度：α = τ / J
    double angularAcceleration1 = impedanceTorque1 / m_J1;
    
    // 2. 积分得到角速度：ω = ∫α dt
    m_calculatedVelocity1 += angularAcceleration1 * dt;
    
    // 3. 积分得到角位置：θ = ∫ω dt
    m_calculatedPosition1 += m_calculatedVelocity1 * dt;
    
    // 4. 计算控制指令
    double commandAngle1 = m_currentAngle1 + m_calculatedPosition1;
    
    // 发送控制指令
    sendControlCommand(commandAngle1, commandAngle2);
}
```

---

## ⚙️ 使用建议

### 转动惯量估算

对于典型的机器人关节：

1. **轻负载关节**
   - J ≈ 0.01 ~ 0.1 kg·m²
   - 适用于小型机械臂末端

2. **中等负载关节**
   - J ≈ 0.1 ~ 1.0 kg·m²
   - 适用于标准工业机器人关节

3. **重负载关节**
   - J ≈ 1.0 ~ 10.0 kg·m²
   - 适用于大型机器人基座关节

### 参数调试步骤

1. **测量或计算转动惯量**
   ```
   J = m × r²
   其中 m 是质量，r 是转动半径
   ```

2. **设置初始参数**
   - K = 10.0（刚度系数）
   - B = 0.5（阻尼系数）
   - G = 0.0（重力补偿）
   - J1 = 估算值
   - J2 = 估算值

3. **观察并调整**
   - 如果回复太慢：增大K，或减小J
   - 如果震荡：增大B
   - 如果有静态偏差：调整G

---

## ⚠️ 注意事项

1. **转动惯量不能为零**
   - 系统会检查并拒绝 J ≤ 0 的值
   - 默认值设为 0.1

2. **时间步长稳定性**
   - 系统自动检测异常时间步长
   - 超过100ms会使用默认值20ms

3. **积分精度**
   - 使用一阶欧拉法，精度适中
   - 如需更高精度，可改用龙格-库塔法

4. **初始化很重要**
   - 每次捕获初始位置时会重置积分状态
   - 确保在静止状态下捕获初始位置

---

## 📈 预期效果

使用新算法后，您应该能观察到：

1. ✅ 更平滑的运动轨迹
2. ✅ 更符合物理规律的力学响应
3. ✅ 更好的控制性能和稳定性
4. ✅ 可针对不同负载调整参数

---

## 🚀 编译和运行

修改完成后，请执行：

1. 清理项目
2. 运行 qmake
3. 重新编译

详见《编译说明.txt》

---

## 📞 技术支持

如有问题，请检查：
- 转动惯量参数是否合理
- 时间步长是否稳定
- 初始位置是否正确捕获
- 控制频率是否正常（50Hz）

祝您使用愉快！ 🎉
